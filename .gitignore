# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# uv cache local (evita cuota en /tmp)
.uv-cache

# LaTeX build artifacts
docs/*.aux
docs/*.log
docs/*.out
docs/*.synctex.gz

# Large generated outputs (optional — descomentar si no quieres versionar PCAPs)
# data/raw/pcap/*.pcapng
# data/processed/*.csv

# Model artifacts (regenerables)
# models/
